{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "npm ci --prefix pup-focus --no-audit --no-fund",
  "buildCommand": "npm --prefix pup-focus run build && rm -rf .next && mkdir -p .next && cp -R pup-focus/.next/. .next/ && cp pup-focus/.next/routes-manifest.json .next/routes-manifest-deterministic.json"
}
